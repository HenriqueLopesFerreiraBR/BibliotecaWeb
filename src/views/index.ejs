<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tela inicial</title>
</head>
<body>
    <h1>Tela inicial</h1>

    <div class="container">
        <div class="content">
            <%if(categoria.length > 0 ) { %> 
                <% categoria.forEach(categoria =>{ %>
                    <h3 class="title"> 
                        <%= categoria.Nome%>
                    </h3>
                <% })%>
            <%}else{%>
                <p>Não a categorias registradas</p>
            <% }%>
        </div>
    </div>
</body>
</html>